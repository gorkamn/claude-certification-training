AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  TechCo Support Agent — unified stack
  Single CloudFront distribution serving both single-agent and multi-agent backends:
    /api-single* → Lambda@Edge signer → single-agent Lambda Function URL
    /api-multi*  → Lambda@Edge signer → multi-agent Lambda Function URL
    /*           → S3 (React static app)

Parameters:
  ClaudeModel:
    Type: String
    Default: us.anthropic.claude-sonnet-4-6
    Description: Bedrock cross-region inference profile ID.
  RefundThreshold:
    Type: String
    Default: "500"
    Description: Dollar threshold above which refunds are blocked by the hook
  AnthropicApiKey:
    Type: String
    NoEcho: true
    Default: ""
    Description: Anthropic API key (leave blank if using Bedrock)
  HumanEscalationEmail:
    Type: String
    Default: "support@example.com"
    Description: Email for human escalation tickets

Globals:
  Function:
    MemorySize: 512

Resources:

  # ─── Single-agent Lambda ───────────────────────────────────────────────────
  AgentFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "${AWS::StackName}-agent"
      Runtime: python3.13
      Architectures: [x86_64]
      CodeUri: ../01-agent-loop/ui/backend/
      Handler: lambda_handler.lambda_handler
      Timeout: 60
      Description: Runs single-agent certification demo scenarios
      Environment:
        Variables:
          CLAUDE_MODEL: !Ref ClaudeModel
          REFUND_THRESHOLD: !Ref RefundThreshold
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub "arn:aws:bedrock:*::foundation-model/*"
                - !Sub "arn:aws:bedrock:*:${AWS::AccountId}:inference-profile/*"
      FunctionUrlConfig:
        # IMPORTANT: Must be AWS_IAM, not NONE. AuthType: NONE triggers Palisade
        # security findings (world-accessible Lambda). The Lambda@Edge signer
        # handles SigV4 signing so browsers don't need IAM credentials.
        AuthType: AWS_IAM

  EdgeSignerSingleInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref AgentFunction
      Action: lambda:InvokeFunctionUrl
      Principal: !GetAtt EdgeSignerSingleFunctionRole.Arn
      FunctionUrlAuthType: AWS_IAM

  EdgeSignerSingleInvokePermission2:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref AgentFunction
      Action: lambda:InvokeFunction
      Principal: !GetAtt EdgeSignerSingleFunctionRole.Arn

  # ─── Multi-agent Lambda ────────────────────────────────────────────────────
  CoordinatorFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "${AWS::StackName}-coordinator"
      Runtime: python3.13
      Architectures: [x86_64]
      CodeUri: ../02-multi-agent/ui/backend/
      Handler: lambda_handler.handler
      Timeout: 120    # Multi-agent runs take longer — 3 parallel subagents
      Description: Runs multi-agent coordinator with trace instrumentation
      Environment:
        Variables:
          CLAUDE_MODEL: !Ref ClaudeModel
          REFUND_THRESHOLD: !Ref RefundThreshold
          ANTHROPIC_API_KEY: !Ref AnthropicApiKey
          HUMAN_ESCALATION_EMAIL: !Ref HumanEscalationEmail
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub "arn:aws:bedrock:*::foundation-model/*"
                - !Sub "arn:aws:bedrock:*:${AWS::AccountId}:inference-profile/*"
      FunctionUrlConfig:
        AuthType: AWS_IAM

  EdgeSignerMultiInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref CoordinatorFunction
      Action: lambda:InvokeFunctionUrl
      Principal: !GetAtt EdgeSignerMultiFunctionRole.Arn
      FunctionUrlAuthType: AWS_IAM

  EdgeSignerMultiInvokePermission2:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref CoordinatorFunction
      Action: lambda:InvokeFunction
      Principal: !GetAtt EdgeSignerMultiFunctionRole.Arn

  # ─── Lambda@Edge — single-agent SigV4 signer ──────────────────────────────
  # WHY Lambda@Edge instead of CloudFront OAC?
  # OAC cannot sign POST request bodies. Lambda Function URLs require the payload
  # hash (x-amz-content-sha256) for POST/PUT, but OAC only signs headers.
  # Browsers can't compute SigV4 signatures, so Lambda@Edge does it at the edge.
  EdgeSignerSingleFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "${AWS::StackName}-edge-signer-single"
      Runtime: nodejs20.x
      CodeUri: edge-signer/
      Handler: index.handler
      MemorySize: 128
      Timeout: 10
      AutoPublishAlias: live
      Description: Signs /api-single* origin requests with SigV4 (origin-request event)
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
                - edgelambda.amazonaws.com
            Action: sts:AssumeRole
      # GOTCHA: Do NOT add Condition: { StringEquals: { lambda:FunctionUrlAuthType: AWS_IAM } }
      # to this policy. It looks correct but causes AccessDeniedException at runtime.
      # Scope by Resource ARN instead.
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - lambda:InvokeFunctionUrl
                - lambda:InvokeFunction
              Resource: !GetAtt AgentFunction.Arn
        - Statement:
            - Effect: Allow
              Action:
                - logs:CreateLogGroup
                - logs:CreateLogStream
                - logs:PutLogEvents
              Resource: "arn:aws:logs:*:*:*"

  # ─── Lambda@Edge — multi-agent SigV4 signer ───────────────────────────────
  EdgeSignerMultiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "${AWS::StackName}-edge-signer-multi"
      Runtime: nodejs20.x
      CodeUri: edge-signer/
      Handler: index.handler
      MemorySize: 128
      Timeout: 10
      AutoPublishAlias: live
      Description: Signs /api-multi* origin requests with SigV4 (origin-request event)
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
                - edgelambda.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - lambda:InvokeFunctionUrl
                - lambda:InvokeFunction
              Resource: !GetAtt CoordinatorFunction.Arn
        - Statement:
            - Effect: Allow
              Action:
                - logs:CreateLogGroup
                - logs:CreateLogStream
                - logs:PutLogEvents
              Resource: "arn:aws:logs:*:*:*"

  # ─── S3 (frontend static files) ──────────────────────────────────────────
  FrontendBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "${AWS::StackName}-frontend-${AWS::AccountId}"
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  FrontendBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref FrontendBucket
      PolicyDocument:
        Statement:
          - Sid: AllowCloudFrontOAC
            Effect: Allow
            Principal:
              Service: cloudfront.amazonaws.com
            Action: s3:GetObject
            Resource: !Sub "arn:aws:s3:::${FrontendBucket}/*"
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub "arn:aws:cloudfront::${AWS::AccountId}:distribution/${CloudFrontDistribution}"

  # ─── CloudFront OAC — S3 ─────────────────────────────────────────────────
  S3OriginAccessControl:
    Type: AWS::CloudFront::OriginAccessControl
    Properties:
      OriginAccessControlConfig:
        Name: !Sub "${AWS::StackName}-s3-oac"
        OriginAccessControlOriginType: s3
        SigningBehavior: always
        SigningProtocol: sigv4

  # ─── CloudFront Distribution ──────────────────────────────────────────────
  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Comment: !Sub "${AWS::StackName} unified UI"
        Enabled: true
        HttpVersion: http2
        DefaultRootObject: index.html
        PriceClass: PriceClass_100

        Origins:
          - Id: S3Origin
            DomainName: !GetAtt FrontendBucket.RegionalDomainName
            S3OriginConfig: {}
            OriginAccessControlId: !GetAtt S3OriginAccessControl.Id

          - Id: SingleAgentOrigin
            DomainName: !Select
              - 2
              - !Split
                - "/"
                - !GetAtt AgentFunctionUrl.FunctionUrl
            CustomOriginConfig:
              OriginProtocolPolicy: https-only
              OriginSSLProtocols: [TLSv1.2]
              OriginReadTimeout: 60

          - Id: MultiAgentOrigin
            DomainName: !Select
              - 2
              - !Split
                - "/"
                - !GetAtt CoordinatorFunctionUrl.FunctionUrl
            CustomOriginConfig:
              OriginProtocolPolicy: https-only
              OriginSSLProtocols: [TLSv1.2]
              OriginReadTimeout: 120    # Match multi-agent Lambda timeout

        DefaultCacheBehavior:
          TargetOriginId: S3Origin
          ViewerProtocolPolicy: redirect-to-https
          AllowedMethods: [GET, HEAD, OPTIONS]
          CachedMethods: [GET, HEAD]
          # Managed-CachingOptimized
          CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6
          Compress: true

        CacheBehaviors:
          - PathPattern: /api-single*
            TargetOriginId: SingleAgentOrigin
            ViewerProtocolPolicy: redirect-to-https
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, PATCH, POST, DELETE]
            CachedMethods: [GET, HEAD]
            # Managed-CachingDisabled (API calls must not be cached)
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            # Managed-AllViewerExceptHostHeader
            OriginRequestPolicyId: b689b0a8-53d0-40ab-baf2-68738e2966ac
            Compress: true
            LambdaFunctionAssociations:
              - EventType: origin-request
                LambdaFunctionARN: !Ref EdgeSignerSingleFunction.Version
                # IMPORTANT: IncludeBody must be true for POST signing.
                # Without it, the edge signer can't read the request body
                # and the payload hash will be wrong → InvalidSignatureException.
                IncludeBody: true

          - PathPattern: /api-multi*
            TargetOriginId: MultiAgentOrigin
            ViewerProtocolPolicy: redirect-to-https
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, PATCH, POST, DELETE]
            CachedMethods: [GET, HEAD]
            # Managed-CachingDisabled
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            # Managed-AllViewerExceptHostHeader
            OriginRequestPolicyId: b689b0a8-53d0-40ab-baf2-68738e2966ac
            Compress: true
            LambdaFunctionAssociations:
              - EventType: origin-request
                LambdaFunctionARN: !Ref EdgeSignerMultiFunction.Version
                IncludeBody: true

        # SPA routing: S3 returns 403 for missing keys (private bucket) → serve index.html
        # GOTCHA: These apply globally to ALL origins, including Lambda. If a Lambda
        # origin returns 403 (e.g., bad SigV4 signature), this silently replaces it with
        # index.html — making it look like a routing problem instead of an auth problem.
        # When debugging API issues, temporarily remove these to see the real error.
        CustomErrorResponses:
          - ErrorCode: 403
            ResponseCode: 200
            ResponsePagePath: /index.html
            ErrorCachingMinTTL: 0
          - ErrorCode: 404
            ResponseCode: 200
            ResponsePagePath: /index.html
            ErrorCachingMinTTL: 0

Outputs:
  CloudFrontUrl:
    Description: CloudFront distribution URL (your app's public URL)
    Value: !Sub "https://${CloudFrontDistribution.DomainName}"

  CloudFrontDistributionId:
    Description: CloudFront distribution ID (for cache invalidation)
    Value: !Ref CloudFrontDistribution

  FrontendBucketName:
    Description: S3 bucket name — sync your frontend build here
    Value: !Ref FrontendBucket
